const yesIfEven = (n) => {
  if (n % 2 === 0) {
    return "yes"
  }
  else return "no"
}

const countXs = (s) => {
  let numOfX=0
  if(s.search('x')){
numOfX++
  }
  return numOfX
}

const containsX = (s) =>{

}